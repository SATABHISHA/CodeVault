# CodeVault

## Deploy to Hostinger

Run this single command from the repository root:

```powershell
.\scripts\deploy_web.ps1
```

This builds Flutter web in release mode and uploads the output directly to `https://scanhub.sroy.es` via SSH — no git branch switching required.